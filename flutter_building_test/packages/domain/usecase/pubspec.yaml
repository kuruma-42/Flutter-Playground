name: usecase
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  repository_data:
    path: ../../data/repository_data
  base_domain: 
    path: ../base_domain 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0